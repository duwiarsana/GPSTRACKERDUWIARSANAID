apiVersion: v1
kind: Service
metadata:
  name: gpstracker-frontend
  namespace: gpstracker
  labels:
    app: gpstracker-frontend
    component: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: gpstracker-frontend

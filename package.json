{
  "name": "gps-tracker-root",
  "private": true,
  "scripts": {
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm start --prefix frontend",
    "dev:all": "concurrently \"npm:dev-backend\" \"npm:dev-frontend\"",
    "setup": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev:up": "docker compose -f docker-compose.dev.yml up -d mysql && npm run dev:all",
    "dev:reset-mysql": "docker rm -f gpstracker-mysql || true && docker compose -f docker-compose.dev.yml up -d mysql",
    "dev:reset-mongo": "npm run dev:reset-mysql"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
